# Example: Plugin workflow declaration
# This file demonstrates how plugins can declare workflows in their manifests
#
# In plugin manifest (manifest.ts):
# workflows: [
#   {
#     id: 'verify',
#     file: 'workflows/mind-verify.yml',
#     description: 'Run Mind verify with default settings',
#     tags: ['verify', 'validation']
#   }
# ]

name: mind-verify
version: 1.0.0
description: Run Mind verify with default settings
on:
  manual: true
jobs:
  verify:
    runsOn: local
    steps:
      - name: Run Mind Verify
        uses: plugin:@kb-labs/mind/cli/verify

